# Ludwitt Learning Engineer (BYOB) — never commit real secrets
LUDWITT_CLIENT_SECRET=
# Optional: defaults to production callback registered in Ludwitt
# For local: http://localhost:3000/auth/callback (must also be registered)
LUDWITT_REDIRECT_URI=https://triniiq-masterclass.vercel.app/auth/callback
# 32+ char secret used to encrypt the Ludwitt session cookie
SESSION_SECRET=
# Optional telemetry upstream override
# TELEMETRY_UPSTREAM_URL=
