name: CI

# Disabled — private repo Actions billing.
on:
  workflow_dispatch:

defaults:
  run:
    working-directory: execution/admissions-take-home

jobs:
  test:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "skipped"
