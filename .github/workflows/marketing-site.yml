name: CI

# Disabled — private repo Actions billing; deploy via Vercel CLI instead.
on:
  workflow_dispatch:

defaults:
  run:
    working-directory: execution/marketing/site

jobs:
  build:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "skipped"
