/** Single source for review-week participant copy. */

export const REVIEW_WEEK_CALLOUT =
  'Evaluate each peer submission, file a written GitHub review on each repository, then cast a private vote. Votes are available only after the written review is recorded.';

export const REVIEW_WEEK_CALLOUT_ENROLLED = `${REVIEW_WEEK_CALLOUT} Your votes are private. After the deadline, the submission with the most votes is selected.`;

export const REVIEW_WEEK_CALLOUT_PUBLIC = `${REVIEW_WEEK_CALLOUT} Votes are private. The submission with the most votes is selected.`;

export const WINNER_SELECTION_PUBLIC =
  'Each participant files a written GitHub review on every other merged submission, then casts a private vote. Votes are available only after the written review is recorded. After review week closes, the submission with the most votes is selected. Live vote totals are not displayed during the review period.';

export const REVIEW_HOW_TO =
  'Work through one peer at a time. For each peer: evaluate their deployment, read their submission pull request, file a written review (GitHub issue), then cast a private vote (available after the review is saved).';

export const WINNER_NOTE =
  'The submission with the most votes after review week is selected. Live vote totals are not displayed during the review period.';

export const VOTE_PRIVACY_NOTE = 'Your vote is private and not visible to other participants.';
