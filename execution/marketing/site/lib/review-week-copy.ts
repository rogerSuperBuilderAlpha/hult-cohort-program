/** Single source for review-week participant copy. */

export const REVIEW_WEEK_CALLOUT =
  'Try every peer build, file a written GitHub review on each repo, then cast a private 👍 or 👎. Votes unlock only after your written review is on file.';

export const REVIEW_WEEK_CALLOUT_ENROLLED = `${REVIEW_WEEK_CALLOUT} Your ratings are private. After the deadline, the repo with the most thumbs up wins.`;

export const REVIEW_WEEK_CALLOUT_PUBLIC = `${REVIEW_WEEK_CALLOUT} Ratings are private. The repo with the most thumbs up wins.`;

export const WINNER_SELECTION_PUBLIC =
  'Each participant files a written GitHub review on every other merged build, then casts a private 👍 or 👎. Votes unlock only after the written review is on file. After review week closes, the repo with the most thumbs up wins. Live tallies are never shown during voting.';
